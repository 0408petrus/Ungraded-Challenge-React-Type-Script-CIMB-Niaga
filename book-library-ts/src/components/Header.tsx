import React from 'react';

const Header: React.FC = () => {
    return (
        <>
        <nav className="navbar navbar-expand-lg navbar-dark bg-primary">
        <a className="navbar-brand p-2" href="#home">Book Library</a>
      </nav>
        </>
    );
};

export default Header;